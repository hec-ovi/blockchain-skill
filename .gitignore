docs/IDEA.md
node_modules/
# Exception: vendored solc lives under dist/vendor/node_modules (required for
# skill-pack installs without a full npm tree). Gitignore otherwise matches any
# path segment named node_modules.
!dist/vendor/
!dist/vendor/**/
!dist/vendor/**/**
!dist/vendor/node_modules/
!dist/vendor/node_modules/**
# Ship the self-contained CLI + vendored solc. Other dist/ artifacts ignored.
dist/*
!dist/agent-wallet.mjs
!dist/vendor/
.env
.env.*
!.env.example
*.log
.wallets/
keystore/
# Scratch for a contract-step walk (spec, threat model, sources, sandbox runs).
.contract-work/
